# Original User Request

## Initial Request — 2026-09-02T13:52:42+03:00

# Teamwork Project Prompt — HAM Portal · Іт.2 (ClickUp + Notifications)

Розширити вже задеплоєний портал заявок HAM: додати синхронізацію задач із ClickUp і сповіщення Gmail/Slack, керовані наявним 12-статусним життєвим циклом — без регресу поточної збірки, тестів і деплою на Render.

Working directory: C:\Users\Administrator\Documents\App-Portal
Integrity mode: development

## Requirements

### R1. Спільний шов (робити першим)
Додати ОДИН event-хук у src/controllers/applicationController.ts — емітити подію на кожен успішний перехід статусу з { app, from, to, actorRole }. Усі фічі підписуються на нього; більше ніхто не редагує логіку переходів контролера. Це єдина зміна спільного файлу.

### R2. ClickUp-інтеграція (src/lib/clickup.ts + webhook-роут)
При переході в статус («Заплановано» / APPROVED / IN_PROGRESS згідно з моделлю) створювати задачу ClickUp з мапінгом полів форми; зберігати clickupTaskId у моделі заявки. Вхідний webhook синхронізує статус ClickUp → портал («На перевірці» → «Очікує перевірку», петля доробок). Конфігурація через змінні оточення (CLICKUP_API_KEY, CLICKUP_LIST_ID, CLICKUP_WEBHOOK_SECRET тощо); м'яка деградація (no-op/logging) без падіння додатку, якщо конфігурація не задана.

### R3. Сповіщення (src/lib/notify.ts, підписка на хук R1)
На кожен перехід статусу надсилати сповіщення за статусною моделлю (POC при новому, Slack DM замовнику при «Очікує перевірку» тощо). Підтримка каналів: Gmail (SMTP/API) та спільний Slack-бот (Webhooks/Bot Token). Конфігурація через змінні оточення; no-op, якщо відповідний канал не налаштований.

### R4. Guardrails та фронтенд-обмеження
У цьому прогоні НЕ рефакторити App.tsx, не змінювати базову логіку пріоритизації/моделі. Зміни фронтенду — мінімальні (лише відображення clickupTaskId / стану сповіщень у деталях заявки, якщо тривіально). DesignShowcase лишається виключеним із прод-бандлу.

### R5. Тестовий Gate & Моки
Кожен коміт має успішно проходити 
pm test (	est/smoke.test.js, 	est/integration/routes.test.js, 	est/unit/sla-and-transitions.test.js) та 
pm run build. Нові модулі (src/lib/clickup.ts, src/lib/notify.ts, webhook routes) покрити модульними/інтеграційними тестами з моками зовнішніх API (ClickUp, Gmail, Slack).

### R6. Гігієна комітів
Conventional Commits (eat:, ix:, 	est:, docs:), малі scoped-коміти, чіткі описи змін.

### R7. Деплой-безпека та сумісність із Render
Чиста збірка під Render (Docker-білд із кореня без помилок). Жодних секретів та API-ключів у коді чи репозиторії. Усі необхідні нові змінні оточення задокументувати у README.md / .env.example.

## Acceptance Criteria

### ClickUp Integration
- [ ] При переході в мапнутий статус створюється задача ClickUp (або спрацьовує мок у тестах) з коректними полями; clickupTaskId зберігається в заявці.
- [ ] Вхідний webhook ClickUp успішно оновлює статус відповідної заявки в порталі та підтримує петлю доробок.

### Notifications
- [ ] Кожен перехід статусу тригерить відповідні сповіщення (Gmail / Slack) згідно зі статусною матрицею.
- [ ] Відсутність конфігурації (env) для ClickUp/Gmail/Slack призводить до безпечного no-op без винятків чи аварійних зупинок сервера.

### Quality & Regression Gates
- [ ] 
pm test і 
pm run build проходять успішно (all green).
- [ ] Нові модулі та роути покриті модульними/інтеграційними тестами.
- [ ] Логіка переходів та пріоритизації не зламана; деплой на Render збирається чисто.
- [ ] У репозиторії відсутні секрети, а змінні оточення описані в README.
